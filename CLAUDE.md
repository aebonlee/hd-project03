# [Claude Code 작업 지시서] 업체 대여 아이템 재고관리 보고서 자동화

- 기획자: 공지은
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 프로젝트 유형: 외부 업체용 웹 포털(재고 실사 입력) + 담당자 승인 워크플로 + 보고서 자동 생성

---

## 0. Claude Code에게 — 작업 방식

1. 기획서가 짧으므로 **9번 "확인 필요 사항"을 먼저 사용자에게 질문**한다. 특히 "기존 보고 양식"(보고서 템플릿)을 받을 수 있는지 확인한다. 받지 못하면 5번 가정의 샘플 양식으로 진행한다.
2. 역할이 두 가지(**업체 실무자**, **HD 담당자**)이므로 화면을 역할별로 분리해 설계한다.
3. 전체 흐름(업체 입력 → 알림 → 담당자 검토 → 보고서 생성)이 샘플 데이터로 끝까지 돌아가는 것을 최우선으로 한다.
4. 부록의 원문이 최종 기준이다.

---

## 1. 한 줄 요약

여러 업체가 대여해 간 아이템(자산)의 **재고 실사 결과를 업체가 직접 웹에서 입력·확인서 업로드**하면 HD 담당자에게 알림이 가고, 담당자가 확인 후 요청하면 **기존 보고 양식대로 수량·금액 일치도 중심의 보고서가 자동 생성**되는 시스템.

## 2. 배경 및 현재 업무 상황

- 홈페이지 등 여러 업체 담당자들이 HD로부터 아이템을 대여해 가며, 주기적으로 실사(재고 확인)를 해야 한다.
- 현재는 업체별로 실사 결과를 받아 담당자가 수작업으로 보고서를 작성하는 것으로 추정된다.
- 보안 중요도는 낮음 → 업체는 **사업자번호로 로그인**하는 단순 인증으로 충분.

## 3. 요구 기능

### 3.1 업체 실무자 화면
- 사업자번호(+ 간단 비밀번호 또는 인증코드)로 로그인.
- 본인 업체가 대여 중인 아이템 목록 표시(아이템명, 대여수량, 단가, 금액, 대여일).
- 아이템별 **실사 수량 입력** → 시스템이 장부 수량과 비교해 일치/불일치 표시.
- **불일치 발생 시 소명(사유) 입력 필수** — 소명 없이는 제출 불가.
- 현재 존재하지 않는 아이템에 대해 **삭제(대여 종료) 요청** 기능 → 담당자 승인 대기 상태로.
- 실사결과 확인서 파일 업로드(PDF/이미지) → 제출 완료.

### 3.2 HD 담당자 화면
- 업체 제출 시 **알림 수신**(메일 또는 문자 — 수단 무관. 1차는 이메일, 어댑터로 교체 가능).
- 업체별 제출 현황 대시보드: 미제출 / 제출 / 불일치 있음 / 승인 완료.
- 제출 상세 검토: 장부 vs 실사 수량, 금액 차이, 소명 내용, 삭제 요청, 첨부 확인서.
- 삭제 요청 승인/반려.
- **[리포트 요청]** 버튼 → 보고서 자동 생성.

### 3.3 보고서 자동 생성
- 기존 보고 양식(엑셀 또는 워드/PDF)에 데이터를 채워 완성.
- 핵심 지표: 업체별·전체 **수량 일치도(%)**, **금액 일치도(%)**, 불일치 건수와 사유 요약, 삭제 요청 내역.
- 출력: 엑셀(+PDF). 양식이 제공되면 템플릿 기반 채우기, 없으면 샘플 양식을 만들어 사용.

### 3.4 기준 데이터 관리
- 담당자가 업체 목록·대여 아이템 목록을 엑셀로 업로드/수정할 수 있는 관리 화면(또는 최소한 CSV 임포트).

## 4. 데이터 스키마 (샘플 데이터 생성 지침)

- **업체(Vendor)**: 사업자번호(로그인 ID), 업체명, 담당자명, 연락처, 이메일
- **대여 아이템(RentalItem)**: 아이템ID, 업체 사업자번호, 아이템명, 규격, 대여수량(장부), 단가, 금액, 대여일, 상태(대여중/종료요청/종료)
- **실사(Audit)**: 실사ID, 업체, 실사 회차(예: 2026-08), 제출일시, 상태(작성중/제출/검토중/승인)
- **실사 상세(AuditLine)**: 실사ID, 아이템ID, 장부수량, 실사수량, 차이, 금액차이, 소명사유, 삭제요청 여부
- **첨부(Attachment)**: 실사ID, 파일명, 경로
- **알림 로그(Notification)**: 대상, 수단, 내용, 발송시각

샘플: 업체 5곳, 업체당 아이템 10~20개, 일부 불일치·삭제 요청 포함.

## 5. 제약사항 및 가정

- 보안 중요도 낮음 → 사업자번호 + 비밀번호(초기값 배포) 수준. 단, 업체 간 데이터는 절대 교차 노출되지 않도록 권한 체크.
- 알림 수단은 이메일로 시작(SMTP/Resend 등). 문자는 추후 어댑터.
- 보고 양식이 없을 경우: "재고 실사 결과 보고서" 샘플 엑셀 양식(표지 요약 + 업체별 시트)을 직접 설계.
- 파일 업로드 용량 제한 10MB, PDF/JPG/PNG.

## 6. 권장 기술 스택 (제안)

- 프론트: HTML/CSS/JS 또는 React(Next.js).
- 백엔드/DB/스토리지/인증: **Supabase**(Postgres + Storage + Row Level Security로 업체별 격리) — 과정에서 이미 Supabase 가입 안내가 있었음.
- 보고서 생성: SheetJS/ExcelJS(브라우저) 또는 Python openpyxl(서버). PDF는 브라우저 인쇄 또는 서버 변환.
- 알림: Supabase Edge Function + 이메일 API.

## 7. 산출물

1. 웹 앱(업체 화면 / 담당자 화면)
2. DB 스키마(SQL) + RLS 정책 + 샘플 데이터 시드
3. 보고서 생성 모듈 + 샘플 보고서 결과물
4. 알림 모듈(이메일, 교체 가능)
5. README(배포, 업체 계정 발급 절차, 보고 양식 교체 방법)

## 8. 구현 단계

- Phase 1: 스키마 + 샘플 데이터 + 업체 로그인/아이템 목록/실사 수량 입력/불일치 소명/제출
- Phase 2: 담당자 현황 대시보드 + 검토 + 삭제 요청 승인 + 제출 알림
- Phase 3: 보고서 자동 생성(수량·금액 일치도) + 파일 업로드
- Phase 4: 기준 데이터 관리 화면, 배포, README

## 9. 확인 필요 사항 (작업 전 질문)

1. **기존 보고 양식 파일**(엑셀/워드)을 제공할 수 있는가? 보고서의 필수 항목은?
2. 아이템 정보에 어떤 필드가 있는가(품명, 규격, 단가, 시리얼 등)?
3. 실사 주기(월/분기)와 업체 수, 업체당 아이템 수 대략?
4. 업체 로그인: 사업자번호만으로 충분한가, 비밀번호도 둘 것인가?
5. 담당자 알림 수단 선호(메일/문자)? 담당자는 1명인가 여러 명인가?
6. "삭제 요청"은 아이템 대여 종료를 의미하는가, 데이터 삭제를 의미하는가?
7. 배포 환경(인터넷 공개 가능 여부)?

---

## 부록. 기획서 원문 (Padlet 게시 내용 그대로)

공지은 - 업체 대여 아이템 재고관리 보고서 자동화

홈페이지 등 여러 업체의 담당자들이 대여해간 아이템을 각 사업자 번호로 로그인하고 (보안 중요도 낮음) 해당하는 아이템 수량을 적어서 완료하고 실사결과 확인서를 업로드 하면, HD담당자에게 알림(메일, 문자 상관없음)을 주고 HD담당자가 이상이 없어 리포트(수량, 금액 별 일치도가 주요인) 요청을 하면 기존 보고양식대로 데이터를 입력하여 완성해주는 자동화를 기획하고자 합니다.

변수로 재고 불일치 발생의 경우 반드시 소명을 해야 하고 현재 존재하지 않으면 업체가 삭제 요청을 하는 기능 등, 업체 실무자가 이용할 수 있게 UI를 구현하면 좋을 것 같습니다.
